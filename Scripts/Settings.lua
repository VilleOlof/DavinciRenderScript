Settings = {}

-- Render.lua settings
FileDestination = ''
RenderExplorerPath = ''
RenderCustomName = '' -- Leave string empty for it to take project name instead

--Marker Color Presets For Render
Default_RenderPreset = 'YouTube - 1080p' -- "Default Preset" if no valid markers
Red_RenderPreset = 'YouTube - 1080p' 
Yellow_RenderPreset = 'YouTube - 1080p'
Green_RenderPreset = 'YouTube - 1080p'